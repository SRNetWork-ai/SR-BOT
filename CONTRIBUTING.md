# مشارکت در SR-BOT

ممنون که می‌خواهید کمک کنید. این راهنما کوتاه است؛ لطفاً کامل بخوانید.

## محیط توسعه

- PHP 8.1+ با افزونه‌های `pdo_mysql`, `curl`, `mbstring`, `json`, `zip`
- MariaDB / MySQL 5.7+
- بدون Composer، Node یا Docker — پروژه باید روی هاست اشتراکی ساده هم کار کند

```bash
cp config.sample.php config.php   # مقادیر دیتابیس و توکن تست را پر کنید
bash tools/lint.sh                # php -l روی همهٔ فایل‌ها
php cli.php migrate               # اجرای مایگریشن‌ها
```

## ساختار مهم

| مسیر | نقش |
|---|---|
| `app/Bot/` | منطق ربات (Bot.php)، مدیریت داخل ربات (AdminBot/AdminHub)، کیبوردها (Kb) |
| `app/Service/` | سرویس‌ها: کیف پول، نمایندگی، به‌روزرسانی (Updater)، دکمه‌ها (Btn)، بکاپ، لاگ … |
| `app/Panel/` | اتصال به پنل‌ها: `Xui` (x-ui/3x-ui)، `Xui3`، `Marzban`، `PasarGuard` |
| `admin/pages/` | هر صفحهٔ پنل وب یک فایل (اکشن‌های POST بالا، HTML پایین) |
| `miniapp/` | مینی‌اپ تلگرام و پنل نمایندگی |
| `database/migrations/` | مایگریشن‌های `NNNN_name.sql` — فقط اضافه کنید، هرگز فایل قبلی را تغییر ندهید |
| `cron/tasks.php` | وظایف دوره‌ای |

## قواعد کد

1. **PHP خالص، بدون وابستگی تازه.** اگر به کتابخانه نیاز دارید اول Issue بزنید.
2. هر ورودی کاربر با `h()` چاپ شود؛ هر کوئری با پارامتر (`DB::all/one/val` + `?`).
3. هر فرم POST پنل وب: `csrf_field()` + `csrf_ok()` + `need('perm.key', 'page')`.
4. تنظیم تازه = کلید در جدول `settings` با پیشوند بخش (`btn_`, `update_`, `rel_`, `cus_` …) و مقدار پیش‌فرض در کد.
5. متن ربات و پنل فارسی با نیم‌فاصله (`‌`)؛ اعداد با `fa_num()` در پنل.
6. مایگریشن باید قابل اجرای دوباره باشد (`CREATE TABLE IF NOT EXISTS`، افزودن ستون با بررسی `information_schema`).
7. دکمهٔ تازهٔ ربات: یک `case 'key':` در `Bot::runButton()` + یک سطر در `Btn::BUILTIN`. اسکن خودکار دکمه‌ها (پنل ← دکمه‌های ربات ← اسکن) موارد جامانده را نشان می‌دهد.

## جریان کار

1. از `main` یک شاخه بسازید: `fix/...` یا `feat/...`
2. کامیت‌ها کوتاه و با پیشوند: `fix:`, `feat:`, `ui:`, `db:`, `docs:`
3. یک بلوک به بالای `CHANGELOG.md` (زیر بیلد جاری) اضافه کنید — ربات همین بلوک را در تاپیک «به‌روزرسانی» اعلام می‌کند.
4. `bash tools/lint.sh` را اجرا کنید؛ CI هم روی PHP 8.1 تا 8.3 همین کار را می‌کند.
5. Pull Request با قالب آماده باز کنید.

## انتشار نسخه

به [`docs/RELEASE.md`](docs/RELEASE.md) مراجعه کنید.
