# سیاست امنیتی / Security Policy

## گزارش آسیب‌پذیری

لطفاً مشکلات امنیتی را **در Issue عمومی ثبت نکنید**.

1. از مسیر **Security → Report a vulnerability** در همین مخزن (GitHub Private Vulnerability Reporting) استفاده کنید.
2. اگر این گزینه فعال نبود، به مالک مخزن در تلگرام پیام خصوصی بدهید (آیدی در صفحهٔ پروفایل مخزن).
3. در گزارش بنویسید: نسخه/بیلد (از `version.json`)، مسیر و فایل مربوط، مراحل بازتولید، و اثر احتمالی.

ما تلاش می‌کنیم ظرف **۷۲ ساعت** پاسخ اولیه بدهیم و وصله را در اولین بیلد بعدی منتشر کنیم. نام گزارش‌دهنده (با اجازهٔ خودش) در CHANGELOG ذکر می‌شود.

## نسخه‌های پشتیبانی‌شده

| نسخه | وضعیت |
|---|---|
| آخرین بیلد شاخهٔ `main` | ✅ پشتیبانی می‌شود |
| بیلدهای قدیمی‌تر (`fixedNN` قبلی) | ❌ فقط با به‌روزرسانی به آخرین بیلد |

## چه چیزهایی در محدودهٔ بررسی است

- دور زدن احراز هویت پنل وب / 2FA / نشست‌ها
- SQL injection، XSS، CSRF در پنل وب، مینی‌اپ و صفحهٔ اشتراک (`sub.php`)
- افشای توکن ربات، اطلاعات پنل‌ها یا فایل‌های `storage/`
- دستکاری موجودی کیف پول / تایید جعلی پرداخت (IPNها)
- مسیر به‌روزرسانی (جایگزینی فایل‌ها / zip slip)

## سخت‌سازی پیشنهادی برای مدیران

- پوشهٔ `install/` را پس از نصب حذف کنید.
- ورود دومرحله‌ای پنل وب را از **تنظیمات ← پیشرفته** فعال کنید و برای هر مدیر شناسهٔ تلگرام ثبت کنید.
- `config.php` و `storage/` را در وب‌سرور بسته نگه دارید (فایل‌های `.htaccess` و `nginx.conf.sample` همراه پروژه).
- برای مخزن خصوصی در به‌روزرسانی خودکار، فقط توکن **Fine-grained** با دسترسی `Contents: Read` بسازید.
- وب‌هوک تلگرام با `secret_token` محافظت می‌شود؛ آن را در `config.php` تغییر ندهید مگر با تنظیم دوبارهٔ وب‌هوک.
