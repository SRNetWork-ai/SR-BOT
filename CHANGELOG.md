# تاریخچه تغییرات SR-BOT

همه بسته‌ها بر پایه نسخه v0.0.1-beta هستند؛ شماره fixedNN ترتیب بسته‌های اصلاحی داخلی است.
فایل version.json دست‌نخورده باقی می‌ماند.

<!-- #changelog-sync -->
## fixed81 → fixed144 — چرخهٔ ۰.۰.۲ (در حال توسعه)

- در صفحهٔ «افزودن به Happ» دیگر آدرس خام اشتراک نمایش داده نمی‌شود؛ فقط لینک رمزنگاری‌شدهٔ پنل تحویل می‌شود
- لینک رمزنگاری‌شدهٔ Happ (happ://crypt5) از خودِ پنل گرفته می‌شود: درخواست POST و خواندن کلید encryptedLink
- پیام خطای پنل (مثلاً طولانی‌بودن آدرس اشتراک) در لاگ ثبت می‌شود
- رفع باگ: دیپ‌لینک Happ با قالب درست (happ://add/ + آدرس خام اشتراک) ساخته می‌شود تا در برنامه Invalid نخورد
- لینک برگشتی از پنل اعتبارسنجی و در صورت نیاز به دیپ‌لینک معتبر تبدیل می‌شود
- در صفحهٔ «افزودن به Happ» آدرس خام اشتراک پنل هم برای افزودن دستی نمایش داده می‌شود
- رفع باگ: حالت تحویل محصول (فقط لینک اشتراک / فقط کانفیگ) اکنون در ربات و مینی‌اپ کامل رعایت می‌شود
- رفع باگ: حذف کانفیگ‌های تکراری و نمایش دقیقاً همان کانفیگ‌هایی که روی پنل ثبت است
- محدودیت هاردویر (HWID): تحویل ساب خودِ پنل و لینک اختصاصی Happ به‌جای ساب داخلی ربات
- تضمین حالت «فقط لینک هپ» در ربات و API مینی‌اپ
- مینی‌اپ: برای محصولات «فقط لینک هپ» فقط لینک Happ نمایش داده می‌شود
- حفظ محدودیت هاردویر (HWID) پس از تمدید سرویس
- حالت تحویل «فقط لینک هپ» برای محصولات: مشتری فقط دیپ‌لینک اختصاصی Happ می‌گیرد
- محدودیت هاردویر (HWID) هر محصول روی پنل نسل جدید سنایی اعمال می‌شود
- لغو لینک اشتراک برای پنل‌های مرزبان و مرزنشین هم فعال شد
- پشتیبانی از پنل‌های مرزنشین (Marzneshin) و هیدیفای (Hiddify)
- مینی‌اپ: نمایش دکمه‌های دستگاه‌ها و لینک‌ها فقط روی سرویس‌های پشتیبانی‌شده
- مینی‌اپ: صفحهٔ دستگاه‌های من (HWID) و صفحهٔ لینک‌ها و Happ در جزئیات سرویس
- اجرای اختیاری با داکر: Dockerfile، docker-compose.yml (nginx + PHP-FPM + MariaDB + cron)، راهنمای docs/DOCKER.md و بخش داکر در README
- مستندسازی نصاب خودکار: راهنمای docs/INSTALL-SR-UI.md، بخش نصب تک‌خطی در README و اجرایی ماندن install.sh و sr-ui در زیپ ریلیز
- آنلاین‌های چندنودی پنل نسل جدید + نصاب هوشمند install.sh و مدیر خط فرمان sr-ui
- بهینه‌سازی سرعت: فهرست سبک اینباندها (list/slim) و تست سریع زنده‌بودن پنل
- ابزارهای تازهٔ پنل نسل جدید: ریست ترافیک گروهی و نمایش لاگ سرور
- ریست خودکار دوره‌ای حجم برای هر پلن (فیلد reset پنل نسل جدید سنایی) — قابل تنظیم در فرم محصول
- دکمهٔ «افزودن به Happ»: دریافت دیپ‌لینک اختصاصی Happ و لینک‌های خارجی اکانت از پنل 3x-ui در ربات و مینی‌اپ.
- ابزارهای پنل 3x-ui: بررسی نسخهٔ پنل، پاک‌سازی اکانت‌های بی‌صاحب و نمایش آمار زندهٔ کاربران و وضعیت محدودیت IP در صفحهٔ سلامت.
- دستگاه‌های من: مشاهده و حذف دستگاه‌های ثبت‌شده (HWID) در ربات و مینی‌اپ برای پنل‌های 3x-ui.
- قابلیت‌های تازهٔ 3x-ui: مدیریت دستگاه‌ها (HWID)، آمار کاربران پنل، صفر کردن گروهی ترافیک، حذف اکانت‌های بی‌صاحب و بررسی امکان محدودیت IP.
- 🛡 سخت‌سازی ۰.۰.۲ (گام ۱۲): جلوگیری از پردازش تکراری آپدیت‌های تلگرام (update_id) تا خرید، تمدید یا شارژ دوبار انجام نشود.
- 🛡 سخت‌سازی امنیتی ۰.۰.۲ (گام ۱۰): عبور همهٔ فرم‌های آپلود پنل از فیلتر امن Upload (نام، پسوند و حجم) و افزودن گزینهٔ ساخت خودکار رمز بکاپ.
- 🛡 سخت‌سازی امنیتی ۰.۰.۲ (گام ۹): محافظ SSRF برای همهٔ درخواست‌های خروجی، محدود کردن ریدایرکت به http/https و نمایش وضعیت آن در صفحهٔ سلامت.
- 🛡 سخت‌سازی امنیتی ۰.۰.۲ (گام ۸): فعال شدن سطح سختگیرانهٔ سپر CSRF برای همهٔ فرم‌های پنل، سرویس تازهٔ Upload برای بررسی نام، پسوند و حجم فایل‌های آپلودی و بستن اجرای اسکریپت در پوشهٔ آپلود.
- 🛡 سخت‌سازی امنیتی ۰.۰.۲ (گام ۷): ساخت خودکار توکن امنیتی وب‌هوک تلگرام، رمزدار شدن همیشگی فایل‌های بکاپ با AES-256 (رمز خودکار و قابل تغییر در صفحهٔ بکاپ) و افزودن دو بررسی تازه به صفحهٔ سلامت سیستم.
- 🛡 سخت‌سازی امنیتی ۰.۰.۲ (گام ۶): اعتبارسنجی سخت‌گیرانهٔ initData مینی‌اپ (اجباری شدن auth_date و پنجرهٔ اعتبار قابل تنظیم با ma_init_ttl_min)، محدودیت نرخ درخواست بر پایهٔ شناسهٔ تلگرام (ma_rate_per_min) و هشدار امنیتی هنگام تلاش‌های ناموفق پی‌درپی برای ورود به مینی‌اپ.
- 🛡 سخت‌سازی امنیتی ۰.۰.۲ (گام ۵): سپر سراسری CSRF روی همهٔ درخواست‌های POST پنل (حالت گزارشی و قابل سخت‌گیرانه شدن با sec_csrf_strict)، مبنا قرار گرفتن آی‌پی واقعی کاربر در قفل ضد حدس رمز و ارسال پیام امنیتی به تاپیک تلگرام هنگام ورود به پنل یا درخواست بدون توکن.
- 🔒 سخت‌سازی امنیتی ۰.۰.۲ (گام ۴): نشست‌های پنل مدیریت امن‌تر شدند — کوکی نشست HttpOnly و SameSite=Lax و روی HTTPS با پرچم Secure، حالت سخت‌گیرانهٔ شناسهٔ نشست، تعویض خودکار شناسه پس از ورود و انقضای نشست بی‌کار پس از ۱۲ ساعت.
- 🛡 سخت‌سازی امنیتی ۰.۰.۲ (گام ۳): زیرساخت محدودیت نرخ درخواست اضافه شد؛ پنل مدیریت در هر دقیقه حداکثر ۳۰۰ درخواست از هر آی‌پی می‌پذیرد و وب‌هوک ربات هرگز محدود نمی‌شود؛ قفل ضد حدس رمز برای ورود به پنل آماده شد (۵ تلاش ناموفق ← ۱۵ دقیقه قفل).
- 🔐 سخت‌سازی امنیتی ۰.۰.۲ (گام ۲): توکن ربات‌ها، کلید درگاه‌های پرداخت و رمزهای ذخیره‌شده در تنظیمات با AES-256-GCM رمزنگاری می‌شوند؛ کلید رمزنگاری خودکار ساخته و در config.php (یا storage/.appkey) نگهداری می‌شود، اسرار قدیمی یک‌بار به‌صورت خودکار رمزنگاری می‌شوند و وضعیت آن در صفحهٔ سلامت سیستم نمایش داده می‌شود.
- 🔐 سخت‌سازی امنیتی ۰.۰.۲ (گام ۱): هدرهای امنیتی روی همهٔ صفحه‌ها و بستن فریم برای پنل مدیریت، بستن دسترسی وب به پوشه‌های app/cron/database/storage/tools/docs، نمونهٔ nginx کامل‌تر با لینک زیبای ساب، قفل خودکار نصاب پس از نصب و بررسی خودکار «فایل‌های حساس روی وب» در صفحهٔ سلامت سیستم.
- 🗂 لاگ تاپیک تلگرام بازنویسی شد: ترمیم خودکار تاپیک حذف/بسته‌شده، تلاش مجدد هنگام محدودیت نرخ تلگرام، صف آفلاین و ارسال دوباره با کرانجاب، جلوگیری از پیام تکراری، ارسال بی‌صدا، تقسیم پیام‌های بلند، تست تک‌تاپیک و دکمهٔ «بررسی و ترمیم تاپیک‌ها» در تنظیمات.
- 🧾 صف رسیدها اصلاح شد: رسیدی که در ربات تایید/رد می‌شود دیگر در «در انتظار» پنل وب نمی‌ماند، فاکتورهای درگاه خودکار (هوش‌پی/نوپیمنتس) از صف رسیدهای دستی جدا و به تب «درگاه خودکار» منتقل شدند (با استعلام دستی، استعلام خودکار کرون و لغو فاکتورهای پرداخت‌نشده)، و بعد از تصمیم‌گیری دکمه‌های تایید/رد از کارت ربات حذف شده و «✅ تایید شده» یا «❌ رد شده» روی همان پیام مهر می‌خورد؛ تایید از پنل وب هم به کاربر اطلاع می‌دهد.
- 🧹 حذف کانفیگ‌های قطع و عودت وجه در ربات و مینی‌اپ: دکمهٔ «حذف کانفیگ‌های قطع» برای پاک‌سازی گروهی کانفیگ‌های منقضی/غیرفعال/حجم‌تمام‌شده و دکمهٔ «حذف سرویس و عودت وجه» در جزئیات سرویس ربات، با محاسبهٔ مبلغ عودتی طبق سیاست مدیر و گزینهٔ جدید «usr_dead_del» در پنل
- 🧩 رفع خطای «json: cannot unmarshal number into Go struct field .id of type string» در تمدید/ویرایش سرویس: شمارهٔ ردیف پنل دیگر به‌جای UUID فرستاده نمی‌شود و نوع فیلدهای حجم/انقضا/فعال‌بودن پیش از ارسال هم‌سان می‌شود
- 🔄 به‌روزرسان هوشمند گیت‌هاب: آدرس کامل مخزن پذیرفته می‌شود، شاخهٔ پیش‌فرض و پوشهٔ داخلی خودکار پیدا می‌شود و خطای «فایل version.json در مخزن نیست» با جدول جزئیات بررسی ریشه‌یابی می‌شود
- 🧮 مقایسهٔ نسخه روی شمارهٔ بیلد (fixedNN) انجام می‌شود؛ نسخهٔ نمایشی 0.0.1-beta قفل می‌ماند
- 🎛 صفحهٔ دکمه‌های ربات از نو نوشته شد: چهار تب چیدمان / فهرست / اسکن / تنظیمات، ویرایشگر دکمه، کارهای گروهی کامل و پیش‌نمایش زنده
- 🔎 اسکن خودکار دکمه‌ها: کد ربات خوانده می‌شود، دکمهٔ جامانده یا مرده گزارش و دکمهٔ تازه خودکار اضافه می‌شود (پس از هر به‌روزرسانی، روزانه در کران‌جاب و با دکمهٔ «اسکن الان»)
- 📱 دکمهٔ مینی‌اپ در فهرست دکمه‌ها حالا واقعاً کار می‌کند
- 🛡 صفحهٔ مدیران: نمایش موبایل اصلاح شد، نشان وضعیت ورود دومرحله‌ای، هشدار مدیر بدون شناسهٔ تلگرام و راهنمای 2FA
- 📦 فایل‌های مخزن گیت‌هاب: .gitignore، CI (php -l روی PHP 8.1–8.3)، ریلیز خودکار با تگ، قالب Issue/PR، SECURITY و CONTRIBUTING، README تازه
- 🏪 انبار ملی به مینی‌اپ اضافه شد (بسته‌ها، خرید فوری، خریدهای من، دریافت مجدد)
- 🗂 حذف جدول قدیمی پنل‌ها که همزمان با کارت‌های جدید نمایش داده می‌شد
- 💳 پنل سه‌بعدی کارت در مینی‌اپ با چرخش روی/پشت کارت
- 📷 امکان آپلود تصویر کارت (JPG/PNG تا ۴ مگابایت) با ذخیره‌سازی امن
- 🔒 محدودیت تک‌کارت: تا کارت فعلی حذف نشود، کارت دوم ثبت نمی‌شود
- ⚙️ سه تنظیم تازهٔ احراز کارت در پنل مدیریت
- 🗃 مدیریت گروهی بکاپ: انتخاب چندتایی، حذف گروهی و ارسال گروهی
- 📦 بکاپ پیش از به‌روزرسانی حالا خود فایل را می‌فرستد (نه لینک)
- 🛠 زیرمنوی پنل مدیریت و نمایندگی قابل کنترل از صفحهٔ دکمه‌ها
- 🔑 قفل نسخهٔ نمایشی روی 0.0.1-beta

## fixed80 — به‌روزرسان هوشمند، دکمه‌های ربات با اسکن خودکار، UI مدیران، فایل‌های ریپو

- به‌روزرسان (Updater): آدرس کامل گیت‌هاب یا owner/repo هر دو پذیرفته می‌شود (normalizeRepo)؛ شاخهٔ پیش‌فرض مخزن و پوشهٔ داخلی حاوی version.json خودکار کشف می‌شود؛ تب «منبع» و «جزئیات بررسی» (آدرس، کد HTTP، راهنمای رفع) به صفحهٔ به‌روزرسانی اضافه شد
- خطای «فایل version.json در مخزن نیست یا شاخه اشتباه است» دیگر بی‌دلیل نیست: علت واقعی (مخزن خصوصی بدون توکن، شاخهٔ اشتباه، زیرپوشه) نمایش داده می‌شود
- مقایسهٔ نسخه روی شمارهٔ بیلد fixedNN (Updater::isNewer)؛ نسخهٔ نمایشی قفل می‌ماند؛ ریشهٔ بسته هنگام نصب از محل app/bootstrap.php پیدا می‌شود (بستهٔ دارای پوشهٔ والد هم درست نصب می‌شود)
- دکمهٔ درونی «مینی‌اپ» در ربات هندلر نداشت و اضافه شد
- موتور اسکن دکمه‌ها (Btn::scanHandlers / scanReport / autoScan): کد Bot.php خوانده و با فهرست دکمه‌های درونی مقایسه می‌شود؛ دکمهٔ تازه خودکار (خاموش) اضافه و دکمهٔ بدون هندلر گزارش می‌شود؛ اجرا پس از هر به‌روزرسانی، روزانه در کران‌جاب و هنگام باز کردن صفحه (با کش)
- صفحهٔ دکمه‌های ربات کامل بازنویسی شد: تب چیدمان (کشیدنی + لمسی، هر منو جدا، بازگردانی، پیش‌نمایش گوشی)، تب فهرست (فیلتر منو/نوع/مخاطب/وضعیت، کارهای گروهی روشن/خاموش/حذف/عرض/رنگ/منو/مخاطب)، تب اسکن (وضعیت هر کلید، دکمه‌های کشف‌شده، سلامت و اصلاح خودکار)، تب تنظیمات (حالت کیبورد، تعداد در ردیف، آدرس مینی‌اپ، خروجی/ورودی JSON، بازنشانی)
- ویرایشگر دکمه با پیش‌نمایش زندهٔ برچسب، انواع لینک/متن/مینی‌اپ/منو/کپی/اشتراک‌گذاری/مخاطب/موقعیت و تکثیر دکمه؛ تنظیمات و دکمه‌های قبلی حفظ می‌شوند
- صفحهٔ مدیران: نمایش موبایل (کارت‌ها، دکمه‌ها و دسترسی‌ها دیگر از کادر بیرون نمی‌زنند)، نشان وضعیت 2FA در سربرگ (لینک به تنظیمات)، هشدار برای مدیر بدون شناسهٔ تلگرام و راهنمای گرفتن شناسه؛ اصلاح تایپی «تنطیم»
- پنل وب: باز شدن مستقیم تب با لینک (#tab-<نام>) مثل تنظیمات ← پیشرفته
- فایل‌های مخزن گیت‌هاب: .gitignore، .gitattributes، ورک‌فلوی CI (php -l روی PHP 8.1 تا 8.3 + PHPStan)، ورک‌فلوی ریلیز (تگ v* ← zip + SHA256SUMS + GitHub Release)، قالب Issue/PR، SECURITY.md، CONTRIBUTING.md، docs/RELEASE.md، README تازه و پوشه‌های storage/{updates,backups,logs}
- version.json: build=fixed80، released=2026-09-08 (version/version_locked/pinned دست‌نخورده)

## fixed79 — بستهٔ بزرگ فاز بعدی: ۱۷ قابلیت + حجم تست مگابایتی + اعلان خودکار آپدیت در تاپیک

- حجم اکانت تست بر حسب مگابایت (ستون panels.test_volume_mb) در ربات، مینی‌اپ و پنل وب؛ مثلاً ۵۰۰ مگابایت
- اعلان خودکار هر بسته در تاپیک آپدیت: ربات بالای CHANGELOG.md را می‌خواند و هر fixedNN را فقط یک‌بار منتشر می‌کند (کران + دکمهٔ دستی در به‌روزرسانی و تنظیمات)
- پاسارگارد: انتخاب تیک‌زدنی گروه‌ها/اینباندها از لیست در «تست اتصال» پنل و ثبت یک‌کلیکی
- پاسارگارد: گروه به‌ازای هر محصول (فیلد گروه‌ها در فرم محصول)
- وضعیت نودها در تست اتصال پاسارگارد/مرزبان + هشدار قطع نود در گروه گزارشات
- مرزبان: قطع/وصل سرویس، آخرین اتصال و لغو لینک اشتراک
- کمپین/تخفیف روی خرید حجم و زمان دلخواه (پیش‌فاکتور و کسر موجودی)
- تبدیل یک‌کلیکی تنظیمات قدیمی حجم دلخواه به محصول (تنظیمات ← فروشگاه)
- دکمهٔ «🧾 سوابق خرید» در ربات (خریدها و تراکنش‌های کیف پول)
- اکانت تست به‌ازای هر سرور با فاصلهٔ جدا و انتخاب سرور تست در مینی‌اپ
- محدودهٔ حجم/زمان هر سرور در خرید دلخواه مینی‌اپ
- نوار مصرف حجم در جزئیات سرویس
- تب «تنظیمات پیشرفته» در پنل وب (تست، تمدید خودکار، حذف خودکار، نودها، لاگ‌ها)
- صفحهٔ «خروجی CSV» در پنل وب با مجوز جدا
- ورود دومرحله‌ای پنل وب با کد تلگرام
- رفع باگ فرم محصول دلخواه (فیلد قیمت پنهان مانع ثبت می‌شد) + حداقل ۱ برای حجم/مدت
- دکمه‌های داخلی تعرفه، کمپین، تمدید، سوابق و تیکت در منوهای فرعی قابل فعال‌سازی از «دکمه‌های ربات»

## fixed78 — پاسارگارد بدون مجوز groups.read + حذف دکمهٔ تکراری «حجم دلخواه» از مرحلهٔ ۱

### 🗂 پاسارگارد: ادمین غیر sudo
- خطای «Permission denied: groups.read (کد 403)» شفاف شد: پیام می‌گوید ادمین مجوز groups.read ندارد و راه حل چیست.
- وقتی لیست گروه‌ها خوانده نمی‌شود، شناسهٔ گروه‌ها از **کاربران موجودِ همان ادمین** (`GET /api/users`) استنتاج می‌شود؛ ساخت کاربر و «تست اتصال» با همین شناسه‌ها کار می‌کنند (با هشدار).
- شناسهٔ عددی در فیلد «گروه‌ها» همیشه بدون نیاز به groups.read کار می‌کند؛ نام گروه فقط با مجوز.
- کلید API با پاسخ «Permission denied» دیگر «نامعتبر» ثبت نمی‌شود.
- «تست اتصال» با ادمینی که به `/api/system` دسترسی ندارد هم موفق است (بدون آمار سیستم).

### 🛒 فروشگاه
- دکمهٔ «📐 حجم و زمان دلخواه» از مرحلهٔ ۱ (انتخاب سرور) حذف شد؛ فقط بعد از انتخاب سرور (و به‌عنوان محصول در دسته) نمایش داده می‌شود.
## fixed77 — گروه پاسارگارد با «نام» + انتخاب سرور اکانت تست

### 🗂 پاسارگارد: گروه با نام یا شناسه
- در فرم پنل (فیلد «گروه‌ها») می‌توان نام گروه‌ها (مثلاً `premium,vip`) یا شناسهٔ عددی آن‌ها را نوشت؛ قبلاً فقط عدد پذیرفته می‌شد و حروف حذف می‌شدند.
- نام‌ها هنگام ساخت کاربر به شناسهٔ واقعی گروه در پنل تبدیل می‌شوند (بدون حساسیت به بزرگی/کوچکی حروف).
- پیام «ساخت کاربر بدون گروه ممکن نیست» حالا علت واقعی را می‌گوید: خطای API/ورود (با کد)، نام گروه اشتباه (با فهرست گروه‌های موجود)، غیرفعال بودن همهٔ گروه‌ها یا نبود گروه در پنل.
- «تست اتصال» همین علت را نشان می‌دهد و پاسخ‌های `data` هم در لیست گروه‌ها پشتیبانی می‌شود.
- جداکنندهٔ فارسی «،» در فیلدهای اینباند/گروه همهٔ پنل‌ها پذیرفته می‌شود.

### 🧪 اکانت تست: انتخاب سرور
- اگر بیش از یک سرور «تست فعال» باشد، ربات فهرست سرورها (پرچم، نام، حجم/مدت تست، سهمیهٔ باقی‌مانده) را نشان می‌دهد و کاربر انتخاب می‌کند (کال‌بک `tst:<panelId>`).
- با یک سرور، رفتار قبلی (ساخت مستقیم) حفظ شده است.
- مینی‌اپ: اکشن جدید `test_panels` و پارامتر اختیاری `panel_id` در اکشن `test`.

## fixed76 — «حجم و زمان دلخواه» به‌عنوان محصول + دکمه‌های جدید ربات + رفع دکمهٔ تکراری پنل مدیریت + پنل وب موبایل‌پسند

### 📐 محصول نوع «حجم و زمان دلخواه» (جدید)
- جدول `products` ستون‌های `type` (fixed/custom)، `price_gb`، `price_day`، `min_gb`، `max_gb`، `min_days`، `max_days` گرفت (Migrate + schema.sql).
- صفحهٔ «محصولات»: فیلد «نوع محصول» + فیلدهای قیمت هر گیگ/هر روز و بازهٔ مجاز (فقط در حالت دلخواه نمایش داده می‌شوند)؛ نشان «📐 حجم و زمان دلخواه» در کارت/جدول و قیمت «از …».
- ربات: محصول دلخواه در لیست سرور/دسته با نشان 📐 می‌آید و با زدن روی آن مستقیم وارد مراحل انتخاب حجم/مدت می‌شود؛ دکمهٔ «📐 حجم و زمان دلخواه» منوی اصلی هم محصول(ها)ی دلخواه را لیست می‌کند (بازه و قیمت برای هر محصول جدا).
- مینی‌اپ: بخش «حجم دلخواه» از محصولات نوع دلخواه تغذیه می‌شود (با نام محصول و بازهٔ اختصاصی)؛ محصول دلخواه در لیست محصولات ثابت تکرار نمی‌شود.
- تمدید (دستی/خودکار) هیچ‌وقت محصول دلخواه را به‌عنوان طرح تمدید پیشنهاد نمی‌دهد.
- تنظیمات قدیمی (فروشگاه ← حجم و زمان دلخواه) فقط تا وقتی هیچ محصول دلخواهی نساخته‌اید استفاده می‌شود (سازگاری کامل).

### 🔘 دکمه‌های ربات
- دکمه‌های درون‌ساخت جدید در «دکمه‌های ربات»: 💰 تعرفه‌ها، 🔥 پیشنهاد ویژه (کمپین)، 🔄 تمدید سرویس، 🎫 تیکت‌های من (پیش‌فرض خاموش).
- رفع دکمهٔ تکراری «پنل مدیریت» / «پنل وب» در منوی مدیریت ربات؛ دکمه‌های درون‌ساخت در هر منو فقط یک‌بار نمایش داده می‌شوند.

### 📱 پنل وب روی موبایل
- کارت پنل‌های افزوده‌شده، خانه‌های آمار (پنل‌ها/محصولات/مدیران/دکمه‌ها/متن‌ها/سلامت)، لینک‌های طولانی و جدول‌ها دیگر از کادر موبایل بیرون نمی‌زنند.

## fixed75 — لاگ اقدامات مدیران (Audit) + ضدسوءاستفادهٔ اکانت تست + کمپین در مینی‌اپ/تمدید

### 📝 لاگ اقدامات مدیران (جدید)
- `app/Service/Audit.php` (جدید): سرویس مرکزی ثبت اقدامات در جدول `logs` (که قبلاً ساخته می‌شد اما هیچ‌وقت پر نمی‌شد). عامل به شکل `web:<username>` / `bot:<tg_id>` / `cron` / `system` ذخیره می‌شود. متادیتا قبل از ثبت پاک‌سازی می‌شود (رمز/توکن/CSRF هرگز ثبت نمی‌شود).
- `admin/pages/audit.php` (جدید) → منوی «🛠 نگهداری سیستم ← 📝 لاگ اقدامات»: فیلتر عامل/جستجو/بازه (۱/۷/۳۰/۹۰ روز/همه)، صفحه‌بندی ۵۰تایی، آمار امروز/هفته/کل و فعال‌ترین مدیران، تنظیم مدت نگهداری (`logs_keep_days`، پیش‌فرض ۹۰ روز) و پاکسازی دستی (فقط سوپرادمین).
- `admin/index.php`: هوک سراسری — هر POST موفق/ناموفق در هر صفحهٔ پنل وب به شکل `web.<page>.<act>` با فیلدهای فرم (پاک‌سازی‌شده)، IP و نتیجه (ok/err/warn) ثبت می‌شود. ورود موفق (`login.ok`) و ناموفق (`login.fail`) هم لاگ می‌شود.
- `app/Service/Perm.php`: مجوز جدید `audit.view` (مدیران با نقش سفارشی باید این مجوز را بگیرند).
- ربات ← ابزارها: دکمهٔ «📝 اقدامات مدیران» (`adm:audit[:<page>]`) با آمار + ۱۰ رکورد در هر صفحه و دکمه‌های قبلی/بعدی.
- ثبت اقدام در ربات: مسدود/آزاد کردن کاربر، تأیید/رد/برگشت شارژ، افزایش/کاهش دستی کیف پول، پیام همگانی، تأیید/رد نماینده، تغییر تنظیمات سریع/عددی، حالت تعمیر، شروع/توقف کمپین، هدیهٔ گروهی، تمدید خودکار، حذف خودکار/پاکسازی سرویس، پاک کردن IPها، خروجی CSV.
- `cron/tasks.php`: نگهداری لاگ‌ها از تنظیم `logs_keep_days` خوانده می‌شود (حداقل ۷ روز).

### 🧪 ضدسوءاستفادهٔ اکانت تست
- `Svc::testAbuseCheck()` (جدید) قبل از ساخت هر تست اجرا می‌شود: سقف کل تست هر کاربر روی همهٔ سرورها (`test_global_max`)، فاصلهٔ زمانی بین دو تست (`test_cooldown_days`)، حداقل عمر حساب (`test_min_age_hours`)، یکتایی شماره موبایل (`test_phone_unique`) و بررسی وجود کلاینت با همان tgId روی پنل (`test_panel_check` — فقط x-ui). هر رد شدن با دلیل در لاگ `test.blocked` ثبت می‌شود (`test_log_block`).
- `Xui::clientsByTgId()` (جدید) روی x-ui.
- ربات ← ابزارها ← «🧪 اکانت تست و ضدسوءاستفاده» (`adm:test`): کلیدهای روشن/خاموش (`adm:tsttog:*`) + تنظیم عددی (`adm:num:tcd|tage|tmax`) + آمار تست امروز/۷ روز و تفکیک دلایل مسدودی.
- پیش‌فرض‌ها: بررسی پنل/یکتایی شماره/لاگ مسدودی = روشن؛ فاصله/حداقل عمر/سقف کل = ۰ (خاموش) — رفتار فعلی تغییر نمی‌کند تا مدیر فعال کند.

### 🎉 کمپین تخفیف در همه‌جا
- `miniapp/api.php`: خرید محصول در مینی‌اپ هم از کمپین فعال استفاده می‌کند (بهترین تخفیف بین کد و کمپین انتخاب می‌شود؛ سفارش با `CAMPAIGN` نشانه‌دار می‌شود).
- `Svc::userRenew()`: تمدید کاربر (ربات و مینی‌اپ) از کمپین نوع «تمدید» استفاده می‌کند؛ مبلغ نهایی/تخفیف در سفارش ثبت می‌شود.

### ℹ️ یادداشت
- «بروزرسانی یک‌کلیکی» (`admin/pages/update.php`) و «نمودارهای داشبورد» از قبل موجود بودند؛ تغییری لازم نبود.
- نیاز به مایگریشن جدید نیست (جدول `logs` در اسکیما وجود دارد).

## fixed74 — رشد و فروش: هدیه/تمدید گروهی + تمدید خودکار از کیف پول + کمپین تخفیف + خروجی CSV + یادآوری چندمرحله‌ای

منوی جدید «🚀 رشد و فروش» در پنل مدیریت ربات (دکمهٔ خانه → `adm:grow`):

- **🔥 کمپین تخفیف زمان‌دار** (`app/Service/Campaign.php`): درصد | مدت (ساعت) | عنوان | سقف تخفیف | محصولات منتخب | شامل تمدید. در فروشگاه ربات بنر و زمان باقی‌مانده نمایش داده می‌شود؛ در خرید و تمدید بیشترین تخفیف (کمپین یا کد) اعمال می‌شود و در سفارش با کد `CAMPAIGN` ثبت می‌شود. پایان خودکار با اتمام زمان + توقف دستی + آمار فروش کمپین.
- **🎁 هدیه / تمدید گروهی** (`app/Service/Bulk.php`): افزودن روز و گیگ به یک دسته (فعال‌ها، نزدیک انقضا، منقضی‌ها، نمایندگان، همه، یا یک سرور مشخص) با پیش‌نمایش تعداد و تأیید دومرحله‌ای؛ اعمال روی پنل (همهٔ نوع پنل‌ها) + دیتابیس؛ فعال‌سازی دوبارهٔ منقضی‌ها؛ پیام هدیه به کاربر (یا بی‌صدا) و گزارش به کانال مدیران.
- **🔁 تمدید خودکار از کیف پول** (`app/Service/AutoRenew.php`): کاربر از «👤 حساب کاربری» روشن/خاموش می‌کند؛ کرون N ساعت قبل از انقضا (یا با رسیدن مصرف به X٪) از کیف پول کسر و با همان طرح (یا ارزان‌ترین طرح همان سرور) تمدید می‌کند؛ در کمبود موجودی یک بار اطلاع می‌دهد (بیت 16). تنظیمات: `arn_enabled`, `arn_hours`, `arn_traffic`. ستون جدید `users.auto_renew` (Migrate + ساخت خودکار).
- **⏰ یادآوری چندمرحله‌ای**: مرحلهٔ دوم هشدار انقضا (`expire_notify_days2`، پیش‌فرض ۱ روز، بیت 32) + دکمهٔ «🔁 فعال‌سازی تمدید خودکار» در هشدارها.
- **🎯 سیستم معرفی** در پنل ربات: آمار کل (لینک‌شده‌ها، معرف‌ها، پاداش پرداختی)، ۵ معرف برتر، روشن/خاموش سریع.
- **📤 خروجی CSV / اکسل** (`app/Service/Export.php`): کاربران، سرویس‌ها، سفارش‌ها، تراکنش‌ها، نمایندگان، نزدیک انقضا (همه یا ۳۰ روز اخیر) — فایل با BOM برای اکسل مستقیم در تلگرام ارسال می‌شود.
- تنظیمات سریع: کلیدهای روشن/خاموش «تمدید خودکار» و «کمپین» + مقادیر عددی `arn_hours`, `arn_traffic`, `expire_notify_days2`.
- فایل‌های تغییریافته: `app/Bot/Bot.php`, `app/Bot/AdminBot.php`, `app/Bot/AdminHub.php`, `app/Service/Migrate.php`, `cron/tasks.php` + ۴ سرویس جدید.

## fixed73 — منوی مدیریت ربات با دسته‌بندی کامل + ابزارهای ادمین + فاز ۳–۴ سنایی جدید

- `app/Bot/AdminHub.php` جدید: مالی، سرویس‌ها، سرورها، ارتباط، ابزار، گزارش‌ها با زیرمنوهای کامل؛ تنظیمات عددی و روشن/خاموش از داخل ربات.
- فاز ۳–۴ سنایی: وضعیت سرور، آنلاین/آخرین اتصال، IPهای کاربر + پاک‌سازی، حذف کلاینت‌های تمام‌شده، لغو لینک سابسکریپشن.

## fixed72 — پشتیبانی کامل پنل پاسارگارد (PasarGuard)

- **درایور جدید `app/Panel/PasarGuard.php`** بر پایهٔ مستندات رسمی docs.pasarguard.org:
  - ورود با **کلید API** (`pg_key_…` — هدر `X-Api-Key`) **یا** یوزر/پسورد ادمین (`/api/admin/token`)؛ نشست کش می‌شود.
  - مدل **گروه‌ها** (کاربر باید عضو حداقل یک Group باشد): لیست گروه‌ها از `/api/groups`، انتخاب گروه از فیلد «کد اینباندها» (= شناسهٔ گروه) و خالی = همهٔ گروه‌های فعال.
  - ساخت کاربر (`POST /api/user` با `group_ids`، `data_limit`، `expire`، `proxy_settings` و بازگشت خودکار بدون proxy_settings در صورت ردشدن)، تمدید/تغییر حجم و زمان (`PUT`)، ریست ترافیک، حذف، فعال/غیرفعال، لغو لینک ساب (`revoke_sub`)، آخرین اتصال (`online_at`)، آمار سیستم (`/api/system`).
  - برخورد با کاربر تکراری (409): به‌روزرسانی + ریست به‌جای خطا؛ لینک اشتراک و کانفیگ‌ها از پاسخ پنل (و در صورت نیاز از خود لینک ساب) خوانده می‌شود.
  - تست سلامت: نسخه، تعداد کاربران/آنلاین، روش ورود و لیست گروه‌ها با شناسه.
- **`Xui.php`**: نوع `pasarguard` از «به‌زودی» به فعال منتقل شد (نام‌های جایگزین: pasargad, pasar-guard, pg)؛ همهٔ عملیات مانند مرزبان به درایور واگذار می‌شود. `limitIds()` برای مرزبان/پاسارگارد فقط یک هدف برمی‌گرداند (جلوگیری از ساخت تکراری وقتی چند شناسه نوشته شده).
- **`Svc.php`**: نام کاربری پاسارگارد مانند مرزبان به `a-z0-9_` (۳ تا ۳۲ کاراکتر) امن می‌شود.
- **پنل وب ← سرورها (`panels.php`)**: راهنمای اختصاصی پاسارگارد + لینک مستندات، فیلد توکن به «کلید API پاسارگارد» تبدیل می‌شود، برچسب فیلد اینباند ← «شناسهٔ گروه‌ها»، یوزر/پسورد با کلید API اختیاری (سمت سرور هم بررسی می‌شود)، نشان «🗂 گروه» در لیست سرورها.
- بدون مایگریشن جدید؛ سرورهای فعلی (سنایی/مرزبان/vpn-ui) تغییری نمی‌کنند.

## fixed71 — رفع مشکل «حجم و زمان دلخواه» + حذف خودکار کانفیگ‌های تمام‌شده
- **پلن دلخواه دیگر بلافاصله «منقضی» نمی‌شود**: پنجرهٔ امن ۳ دقیقه‌ای پس از ساخت در همگام‌سازی — مقادیر پنل روی حجم/زمان ردیف سوار نمی‌شود و اگر پنل اکانت تازه را تمام‌شده نشان دهد، حجم/زمان خرید دوباره روی پنل نوشته می‌شود (Svc::pushLimits). زمان انقضای ثانیه‌ای پنل هم به میلی‌ثانیه یکدست می‌شود.
- **3x-ui / مرزبان — اکانت هم‌نام قدیمی**: اگر هنگام ساخت، اکانتی با همان نام روی پنل باشد، حجم/زمان جدید بازنویسی و مصرف قبلی صفر می‌شود (قبلاً سهمیهٔ تمام‌شدهٔ قدیمی به ارث می‌رسید).
- **دکمهٔ «📐 حجم و زمان دلخواه» در چیدمان دکمه‌های پنل وب**: به دکمه‌های درون‌ساخت اضافه شد؛ روی چیدمان‌های قدیمی خودکار بعد از «محصولات» نمایش داده می‌شود و فقط وقتی قابلیت روشن باشد دیده می‌شود (منوی اصلی، زیرمنوی خرید، حالت شیشه‌ای).
- **حذف خودکار کانفیگ‌های تمام‌شده**: پس از N روز (پیش‌فرض ۳) بدون تمدید، کانفیگ از پنل پاک و به سطل زباله منتقل می‌شود. هشدار ۲۴ ساعت قبل + اطلاع پس از حذف + لاگ در تاپیک سرویس‌ها. تنظیمات: سرویس‌ها ← عملیات ← «🧹 حذف خودکار» (svc_autodel_on / svc_autodel_days / svc_autodel_warn / svc_autodel_notify / svc_autodel_rs). کانفیگ‌های نمایندگی و تست پیش‌فرض مستثنا.
- **ستون جدید services.expired_at**: لحظهٔ تمام‌شدن (زمان یا حجم) — مایگریشن خودکار یا «بروزرسانی ← اجرای مایگریشن».
- **لاگ ساخت سرویس**: مقادیر حجم/زمان/انقضای ارسالی به پنل در لاگ svc ثبت می‌شود. فاصلهٔ «۵ گیگ» در لیست سرویس‌های من اصلاح شد.

## fixed70 — مجموعه امنیت، گزارش‌های تنظیم‌پذیر و ابزار توسعه
- **گزارش‌های دوره‌ای قابل تنظیم**: بازه ساعتی جداگانه برای هر گزارش (پنل‌ها، آماری، فروش، کاربران جدید، پرداخت‌های در انتظار، هشدار قطعی پنل) از تنظیمات ← گزارشات. صفر = خاموش.
- **رمزگذاری بکاپ**: رمز دلخواه برای فایل‌های ZIP بکاپ (AES-256) + بازگردانی فایل رمزدار + نمایش وضعیت رمز در پیام تلگرام.
- **قفل ورود**: پنجره ۲۴ساعته برای شمارش تلاش‌های ناموفق + پاکسازی خودکار قفل‌های منقضی seclock در cron.
- **Flood Control**: محدودساز پیام/کلیک با تنظیمات flood_on / flood_max / flood_win / flood_ban.
- **جوین اجباری**: کش کوتاه‌مدت نتیجه بررسی عضویت (fj_cache_min) + آمار «نمایش دیوار عضویت» و «عضویت تاییدشده» در صفحه جوین اجباری.
- **پنل ادمین**: حذف فونت گوگل (CDN) و استفاده از فونت سیستم — سرعت بیشتر در ایران و حفظ حریم خصوصی.
- **nginx.conf.sample**: بستن مسیرهای حساس (app، storage، cron و غیره) روی nginx.
- **درایور Xui3 — فاز ۲**: متدهای bulkAdjust (تمدید/شارژ گروهی)، onlines، lastOnline، byTgId، setEnabled + نمایش تعداد کاربران آنلاین پنل‌های نسل جدید در گزارش پنل‌ها.
- **مینی‌اپ**: بخش «حجم و زمان دلخواه» (cus_info / cus_buy).
- **ابزار توسعه**: tools/lint.sh (چک سینتکس)، phpstan.neon.dist (تحلیل استاتیک)، docs/refactor-plan.md (نقشه شکستن فایل‌های بزرگ).

## fixed69 — جوین اجباری چندکاناله + استقلال «حجم و زمان دلخواه»
- بخش مستقل «📢 جوین اجباری» در پنل ادمین: تا ۸ کانال، عمومی/خصوصی، محدوده بررسی سه‌حالته، متن دلخواه، تست دسترسی ربات.
- گیت عضویت روی دکمه‌های شیشه‌ای + /start + خرید و اکانت تست.
- «حجم و زمان دلخواه» بدون نیاز به محصول پایه؛ دکمه مستقل در منوی اصلی و داخل هر سرور فروشگاه.

## fixed68 — تعرفه نمایندگی per-server در مینی‌اپ
- قیمت هر گیگ/روز و محدودیت‌های هر سرور در پنل نمایندگی مینی‌اپ + پیش‌فاکتور بر اساس سرور انتخابی. اولویت: شخصی > سرور > عمومی.

## fixed67 — قیمت پلن دلخواه per-panel + حذف پوشه نصب
- جدول قیمت جدا برای هر سرور در «حجم و زمان دلخواه» (cus_panels) + حذف خودکار پوشه install پس از نصب.

## fixed66 — چهار اصلاح
- قیمت اختصاصی پنل در مینی‌اپ، رفع گیر رسیدهای تاییدشده، گزارش پنل‌ها حداکثر روزانه، نسخه اول «حجم و زمان دلخواه».

## fixed65 — دکمه‌های درگاه پرداخت
- مخفی شدن دکمه روش‌های پرداخت خاموش (کارت به کارت و غیره) در همه منوها.

## fixed64 — خوددرمانی دیتابیس
- «🧩 بررسی و تکمیل ساختار» در بخش بروزرسانی برای ستون‌های جامانده (مثل ssl_verify).

## fixed63 — فاز ۱ سناریوی 3X-UI جدید
- درایور Xui3 (توکن Bearer، endpointهای clients/*) + فیلد API Token + تشخیص خودکار نسل پنل.

## fixed62 — اصلاحات امنیتی
- چهار اصلاح بحرانی امنیتی نسخه اولیه.
