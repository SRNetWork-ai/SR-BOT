<div align="center">

# 🛒 SR-BOT — ربات فروش کانفیگ + پنل مدیریت وب

ربات تلگرامی فروش کانفیگ VPN با **پنل مدیریت تحت وب**، **مینی‌اپ**، **سیستم نمایندگی** و اتصال به پنل‌های **x-ui / 3x-ui / Marzban / PasarGuard**

نوشته‌شده با PHP + MySQL — بدون نیاز به Composer، Node یا Docker · نصب روی VPS یا هاست اشتراکی

![PHP](https://img.shields.io/badge/PHP-8.1%20%E2%80%93%208.3-777bb4?logo=php&logoColor=white)
![MySQL](https://img.shields.io/badge/MySQL%20%2F%20MariaDB-5.7%2B-4479a1?logo=mysql&logoColor=white)
![Version](https://img.shields.io/badge/version-0.0.1--beta-orange)
![CI](https://github.com/SRNetWork-ai/SR-BOT/actions/workflows/ci.yml/badge.svg)

</div>

---

## English summary

**SR-BOT** is a self-hosted Telegram shop bot for selling VPN configs (Xray / V2Ray) with a full web admin panel, a Telegram Mini App and a reseller system. It connects to **x-ui / 3x-ui**, **Marzban** and **PasarGuard** panels, sells fixed plans or custom volume/duration, handles wallet top-ups (card-to-card with receipt review, NowPayments crypto, HooshPay), test accounts, renewals, auto-delete of expired configs, per-topic report groups, backups and **one-click self-update from this GitHub repository**. Pure PHP 8 + MySQL, no Composer/Node/Docker; runs on a VPS or shared hosting. Interface language is Persian.

A one-line installer (`install.sh`) provisions nginx, PHP-FPM, MariaDB, cron, firewall and SSL on a fresh Ubuntu/Debian/RHEL server, then installs `sr-ui` - an x-ui-style menu that manages the whole stack from the terminal (status, update, backup/restore, webhook, SSL, domain, logs, uninstall).

---

## ✨ امکانات

### در ربات تلگرام
- فروش خودکار کانفیگ از پنل‌های **x-ui / 3x-ui، Marzban و PasarGuard** (چند سرور همزمان)
- محصولات ثابت + **حجم و زمان دلخواه** (به‌عنوان یک محصول قابل روشن/خاموش برای هر سرور)
- **اکانت تست** (حجم بر حسب مگابایت، انتخاب سرور توسط کاربر، محدودیت زمانی)
- کیف پول: کارت‌به‌کارت با احراز کارت و بررسی رسید، **NowPayments** (ارز دیجیتال)، **HooshPay**، کد هدیه، پورسانت معرفی
- تمدید، افزودن حجم/زمان، دریافت مجدد، QR، لینک ساب اختصاصی و **حذف خودکار کانفیگ‌های منقضی پس از N روز**
- **مینی‌اپ تلگرام** (فروشگاه، سرویس‌ها، کیف پول، انبار ملی، پنل نمایندگی)
- **سیستم نمایندگی** دو سطحی: تعرفهٔ جداگانه برای هر سرور، اعتبار منفی (کردیت)، ربات اختصاصی نماینده، دامنه و برند اختصاصی
- **پنل مدیریت داخل ربات**: کاربران، سرویس‌ها، وضعیت سرورها، بکاپ فوری، تنظیمات سریع، آمار و گزارش‌ها
- **جوین اجباری** کانال، کمپین‌ها، آموزش‌ها، تیکت پشتیبانی، انبار ملی (فروش دستی WireGuard/OpenVPN/اکانت)
- **دکمه‌های ربات کاملاً قابل مدیریت** از پنل وب: چیدمان کشیدنی، افزودن دکمهٔ لینک/متن/مینی‌اپ، رنگ و عرض، **اسکن خودکار** دکمه‌های جامانده

### در پنل مدیریت وب
- داشبورد فروش، مدیریت پنل‌ها (تست اتصال، اینباندها/گروه‌ها، نودها)، محصولات، کاربران، سرویس‌ها، پرداخت‌ها
- **گزارش‌های تاپیک‌دار** در گروه تلگرام (خرید، مالی، تست، پنل‌ها، بکاپ، امنیت، به‌روزرسانی …)
- بکاپ خودکار/دستی و بازگردانی، لاگ‌ها، سلامت سیستم، خروجی اکسل/CSV
- **به‌روزرسانی یک‌دکمه‌ای از گیت‌هاب** (این مخزن) با بکاپ پیش از نصب، مایگریشن خودکار و اعلان در تاپیک به‌روزرسانی
- مدیران با **دسترسی ریزدانه** (۱۷ گروه دسترسی) و **ورود دومرحله‌ای (2FA)** با کد تلگرام
- طراحی **موبایل‌اول** (اندروید/iOS)، حالت روشن/تاریک، جستجوی سریع بخش‌ها با `Ctrl+K`

### پنل‌های پشتیبانی‌شده

| پنل | نسخه | نکته |
|---|---|---|
| **3x-ui** | نسخه‌های 1.x و 2.x | انتخاب اینباند، محدودیت سرعت، externalProxy |
| **x-ui** (قدیمی) | ✔ | |
| **Marzban** | نسخه‌های اخیر | کاربر با پروتکل‌ها/اینباندها، revoke ساب، نودها |
| **PasarGuard** | جدیدترین | ساخت کاربر بر پایهٔ **گروه** (Groups)، نودها، کاربر sudo یا کاربر محدود با دسترسی `groups.read` |

---

## ⚡ نصب تک‌خطی و خودکار (پیشنهادی)

روی یک سرور تازه (Ubuntu 20/22/24، Debian 11/12، CentOS/Alma/Rocky 8 و 9، Fedora) فقط همین یک دستور را با کاربر root بزنید:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/SRNetWork-ai/SR-BOT/main/install.sh)
```

نصاب به‌صورت خودکار: سیستم‌عامل و مدیر بستهٔ آن را تشخیص می‌دهد · nginx، PHP-FPM و افزونه‌های لازم، MariaDB و cron را نصب می‌کند · نسخهٔ PHP را بررسی می‌کند · سرویس و سوکت PHP-FPM را پیدا می‌کند · دیتابیس و کاربر آن را با رمز تصادفی می‌سازد · سورس را از همین مخزن می‌گیرد (اگر `config.php` قبلی باشد حفظ می‌شود) · دسترسی‌ها و SELinux را درست می‌کند · وی‌هاست امن nginx می‌نویسد (`app/`، `database/`، `cron/`، `tools/`، `config.php` و `storage/` بسته می‌شوند) · کران‌جاب پنج‌دقیقه‌ای می‌گذارد · پورت را روی ufw/firewalld باز می‌کند · در صورت دادن دامنه با certbot گواهی SSL می‌گیرد · و در پایان دستور مدیریت `sr-ui` را نصب می‌کند.

در پایان، آدرس `http(s)://دامنه[:پورت]/install/` را باز کنید تا نصاب وب فایل `config.php` و حساب مدیر را بسازد. رمز دیتابیس و خلاصهٔ نصب در `/root/sr-bot-install.txt` و تنطیمات سرور در `/etc/sr-bot/sr-ui.conf` ذخیره می‌شود.

### نصب بی‌سؤال (بدون پرسش)

```bash
SRB_NONINTERACTIVE=1 SRB_DOMAIN=bot.example.com SRB_SSL=1 \
  bash <(curl -fsSL https://raw.githubusercontent.com/SRNetWork-ai/SR-BOT/main/install.sh)
```

| متغیر | پیش‌فرض | کار |
|---|---|---|
| `SRB_DOMAIN` | خالی | دامنهٔ پنل؛ خالی = آی‌پی سرور |
| `SRB_PORT` | `80` | پورت وب |
| `SRB_SSL` | `0` | `1` = گرفتن گواهی با certbot |
| `SRB_ROOT` | `/var/www/sr-bot` | مسیر نصب |
| `SRB_DB_NAME` / `SRB_DB_USER` / `SRB_DB_PASS` | `srbot` / `srbot` / تصادفی | دیتابیس |
| `SRB_DB_PREFIX` | `vs_` | پیشوند جدول‌ها |
| `SRB_REPO` / `SRB_BRANCH` | `SRNetWork-ai/SR-BOT` / `main` | منبع سورس |
| `SRB_NONINTERACTIVE` | `0` | `1` = بدون هیچ پرسشی |

### مدیریت سرور با دستور `sr-ui`

بعد از نصب فقط بنویسید `sr-ui` تا منوی فارسی (شبیه منوی x-ui) باز شود، یا مستقیم:

```bash
sr-ui status            # وضعیت nginx / php-fpm / mysql / cron + نسخه و بیلد + کد HTTP سایت
sr-ui restart           # ری‌استارت nginx و php-fpm
sr-ui update            # بررسی و نصب نسخهٔ جدید از گیت‌هاب + بکاپ و اصلاح دسترسی‌ها
sr-ui backup            # بکاپ دیتابیس یا کامل
sr-ui restore           # بازگردانی از فهرست بکاپ‌ها
sr-ui migrate | check   # مایگریشن / بررسی نسخه
sr-ui webhook | token   # وضعیت و تنطیم وب‌هوک، تغییر توکن ربات
sr-ui ssl | domain      # گواهی SSL، تغییر دامنه و پورت
sr-ui logs | perms | db # لاگ‌ها، اصلاح دسترسی، کنسول دیتابیس
sr-ui uninstall         # حذف کامل (با بکاپ در /root/sr-bot-backups)
```

راهنمای کامل نصاب و همهٔ گزینه‌های `sr-ui`: [docs/INSTALL-SR-UI.md](docs/INSTALL-SR-UI.md)

---

## 🖥 نصب دستی روی سرور (VPS)

روی Ubuntu 20/22/24 یا Debian 11/12:

```bash
# پیش‌نیازها
apt update && apt install -y nginx php-fpm php-mysql php-curl php-mbstring php-zip php-xml mariadb-server unzip cron certbot python3-certbot-nginx

# فایل‌ها
mkdir -p /var/www/srbot && cd /var/www/srbot
unzip /root/SR-BOT-*.zip -d . && mv SR-BOT-*/* . 2>/dev/null; chown -R www-data:www-data /var/www/srbot

# دیتابیس
mysql -e "CREATE DATABASE srbot CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci; CREATE USER 'srbot'@'localhost' IDENTIFIED BY 'STRONG_PASS'; GRANT ALL ON srbot.* TO 'srbot'@'localhost'; FLUSH PRIVILEGES;"

# nginx: نمونهٔ آماده
cp nginx.conf.sample /etc/nginx/sites-available/srbot && ln -s /etc/nginx/sites-available/srbot /etc/nginx/sites-enabled/
# دامنه و مسیر را در فایل ویرایش کنید، بعد:
nginx -t && systemctl reload nginx && certbot --nginx -d bot.example.com
```

سپس در مرورگر `https://bot.example.com/install/` را باز کنید و نصاب وب را دنبال کنید (پایین را ببینید). در پایان **کران‌جاب** را اضافه کنید:

```bash
*/5 * * * * php /var/www/srbot/cron/tasks.php >/dev/null 2>&1
```

---

## 🌐 نصب روی هاست اشتراکی (سی‌پنل / دایرکت‌ادمین)

1. فایل‌ها را در `public_html` (یا پوشهٔ دامنه) آپلود و از حالت زیپ خارج کنید.
2. در سی‌پنل یک **دیتابیس MySQL** و یک **کاربر** بسازید و کاربر را با تمام دسترسی‌ها به دیتابیس وصل کنید.
3. در مرورگر باز کنید: `https://your-domain.com/install/`
4. نصاب وب در ۵ مرحله راهنمایی می‌کند:
   - بررسی پیش‌نیازها (نسخهٔ PHP، افزونه‌ها، دسترسی نوشتن)
   - اطلاعات دیتابیس (با تست اتصال زنده)
   - توکن ربات (اعتبارسنجی خودکار با تلگرام)
   - ساخت حساب مدیر پنل + نام فروشگاه
   - پایان: ساخت `config.php`، ساخت جداول و تنظیم وب‌هوک
5. در بخش **Cron Jobs** هاست، این دستور را با فاصلهٔ ۵ تا ۱۵ دقیقه اضافه کنید:
   ```
   php /home/USER/public_html/cron/tasks.php
   ```
6. پوشهٔ `install` پس از نصب **خودکار** حذف می‌شود؛ اگر باقی ماند دستی حذف کنید.

> هاست باید **PHP 8.1+**، افزونه‌های `pdo_mysql`, `curl`, `mbstring`, `json`, `zip` و دسترسی به `api.telegram.org` داشته باشد. اگر سرور ایران است و تلگرام مسدود است، از هاست/سرور خارج یا پروکسی استفاده کنید.

---

## 🔑 ورود به پنل مدیریت

- آدرس: `https://your-domain.com/admin/`
- یا از داخل ربات: **🛠 پنل مدیریت → ورود به پنل وب** (لینک یک‌بارمصرف ۱۰ دقیقه‌ای)
- **ورود دومرحله‌ای**: تنظیمات → پیشرفته → «ورود دومرحله‌ای پنل وب» را روشن کنید و برای هر مدیر **شناسهٔ تلگرام** ثبت کنید (صفحهٔ مدیران به شما هشدار می‌دهد اگر مدیری شناسه ندارد).

---

## ⚙️ افزودن پنل (x-ui / 3x-ui / Marzban / PasarGuard)

پنل مدیریت → **پنل‌ها** → افزودن:

| فیلد | نمونه |
|---|---|
| نوع پنل | `3x-ui` / `x-ui` / `marzban` / `pasarguard` |
| پروتکل / هاست / پورت | `https` , `panel.example.com` , `2053` |
| مسیر وب (پچ) | `mypath` — اگر ندارید **خالی** |
| یوزر / پسورد | همان اطلاعات ورود پنل |
| لینک ساب | `https://panel.example.com:2096/sub` |
| کد اینباندها / گروه‌ها | x-ui: `1,2` — PasarGuard: نام یا شناسهٔ گروه، یا خالی و انتخاب از فهرست |
| حجم اکانت تست (MB) | `200` — خالی = تست خاموش برای این سرور |
| محدودیت ساخت یوزر | `-1` = نامحدود |

سپس **تست اتصال** را بزنید؛ در صورت موفقیت تعداد اینباندها/گروه‌ها و نودها نمایش داده می‌شود.

> **PasarGuard:** ساخت کاربر بدون گروه ممکن نیست. اول در پنل PasarGuard یک Group با اینباندهای دلخواه بسازید. اگر با کاربر غیر sudo وصل می‌شوید، به آن کاربر دسترسی `groups.read` بدهید یا شناسهٔ گروه را دستی وارد کنید (ربات در صورت خطای 403 گروه را از روی پاسخ پنل استنتاج می‌کند). مستندات: <https://docs.pasarguard.org/en/panel/groups>

---

## ⚡️ درگاه خودکار ارزی (NowPayments)

1. در حساب NowPayments از مسیر `Store settings → API keys` یک کلید API بسازید.
2. از مسیر `Store settings → IPN` مقدار **IPN secret key** را بردارید و آدرس Callback را ثبت کنید: `https://دامنه-شما/nowpay.php`
3. پنل مدیریت → درگاه‌ها → «⚡️ درگاه خودکار ارزی»: کلید API، IPN Secret، ارز پیش‌فرض (مانند `usdttrc20`) و حداقل مبلغ.
4. دکمهٔ «🔌 تست اتصال و کلید» صحت کلید را بررسی می‌کند.

پرداخت‌ها **خودکار** تایید و کیف پول شارژ می‌شود. اگر IPN نرسد، دکمهٔ «🔄 بررسی وضعیت پرداخت» در ربات و پیگیری خودکار کران‌جاب پشتیبان هستند. درگاه **HooshPay** (`hooshpay.php`) هم به همین شکل در صفحهٔ درگاه‌ها تنظیم می‌شود.

---

## 📈 نرخ خودکار ارز (دلار/تتر → تومان یا ریال)

پنل مدیریت → تنظیمات → «📈 نرخ خودکار ارز»:

| تنظیم | توضیح |
|---|---|
| حالت نرخ | `دستی` = عدد ثابت ، `خودکار` = دریافت لحظه‌ای از بازار |
| منبع نرخ | نوبیتکس ، والکس ، تترلند |
| واحد محاسبه | تومان یا ریال (×۱۰) |
| کارمزد/سود | درصدی که روی نرخ بازار اضافه می‌شود |
| رند کردن | مضرب رند شدن عدد نهایی (مثل ۵۰۰) |
| فاصلهٔ به‌روزرسانی | هر چند دقیقه نرخ دوباره گرفته شود |

---

## 🗂 گروه گزارشات تاپیک‌دار

1. یک گروه بسازید و **Topics** را روشن کنید.
2. ربات را عضو و **ادمین** گروه کنید (دسترسی مدیریت تاپیک‌ها لازم است).
3. شناسهٔ گروه (مانند `-1001234567890`) را در پنل مدیریت → تنظیمات → «🗂 گروه گزارشات» وارد کنید.
4. «🧩 ساخت خودکار تاپیک‌ها» را بزنید؛ سپس با «🧪 ارسال پیام تست» بررسی کنید.

تاپیک‌ها: گزارش شبانه، خریدها، مالی، اکانت تست، پنل‌ها، پورسانت، نمایندگان، احراز کارت، کیف پول، امنیت، کران‌جاب، بکاپ، **به‌روزرسانی** (اعلان خودکار هر بیلد تازه) و … . هر رویداد جداگانه خاموش/روشن می‌شود.

---

## 🔄 به‌روزرسانی از گیت‌هاب

پنل مدیریت → **به‌روزرسانی**:

| تب | کار |
|---|---|
| **منبع** | مخزن (`owner/repo` یا آدرس کامل گیت‌هاب)، شاخه (خالی = شاخهٔ پیش‌فرض)، پوشهٔ داخلی (اگر `version.json` در زیرپوشه است)، توکن (فقط مخزن خصوصی) |
| **خانه** | «بررسی نسخهٔ جدید» → مقایسهٔ بیلد فعلی و بیلد مخزن، لیست تغییرات، «جزئیات بررسی» (آدرس، کد HTTP، شاخهٔ کشف‌شده)، نصب یک‌دکمه‌ای |
| **نصب از فایل** | آپلود zip ریلیز |
| **دیتابیس** | مایگریشن‌های اجرا‌شده/اجرا‌نشده |
| **تاریخچه** | نسخه‌های قبلی و بازگشت |

جریان نصب: بکاپ → دریافت zip → جایگزینی فایل‌ها (به‌جز `config.php` و `storage/`) → مایگریشن → اسکن دکمه‌های ربات → اعلان در تاپیک به‌روزرسانی. کران‌جاب هم روزانه نسخهٔ تازه را بررسی و به مدیر اطلاع می‌دهد.

> مخزن خصوصی: یک **Fine-grained token** با مجوز `Contents: Read` بسازید. بدون توکن، پیام «فایل version.json در مخزن نیست» می‌بینید.

خط فرمان:

```bash
php cli.php version        # نسخه و بیلد فعلی
php cli.php check          # بررسی نسخهٔ جدید
php cli.php update         # دریافت و نصب
php cli.php backup [full]  # بکاپ دیتابیس / کامل
php cli.php restore <file> # بازگردانی
php cli.php migrate        # مایگریشن‌های اجرانشده
php cli.php prune 7        # پاک‌سازی بکاپ‌های قدیمی
```

---

## 🗂 ساختار فایل‌ها

```
├── index.php              ورودی وب‌هوک تلگرام
├── rbot.php               وب‌هوک ربات‌های اختصاصی نمایندگان
├── nowpay.php hooshpay.php   ورودی IPN درگاه‌ها
├── sub.php                لینک ساب اختصاصی
├── cli.php                ابزار خط فرمان (بکاپ، به‌روزرسانی، مایگریشن)
├── install.sh             نصاب خودکار سرور (تک‌خطی) + نصب دستور sr-ui
├── install/               نصاب وب (پس از نصب حذف می‌شود)
├── admin/                 پنل مدیریت تحت وب
│   └── pages/             صفحات پنل (داشبورد، پنل‌ها، محصولات، دکمه‌های ربات، به‌روزرسانی …)
├── miniapp/               مینی‌اپ تلگرام و پنل نمایندگی
├── app/
│   ├── bootstrap.php DB.php Tg.php Helpers.php
│   ├── Panel/             Xui.php  Xui3.php  Marzban.php  PasarGuard.php
│   ├── Service/           Updater, Btn, Wallet, Reseller, Backup, Logs, Release …
│   └── Bot/               Bot.php (منطق ربات)، AdminBot / AdminHub (مدیریت داخل ربات)
├── assets/                CSS و JS پنل
├── cron/tasks.php         وظایف دوره‌ای
├── database/              schema.sql + migrations/
├── tools/                 sr-ui (مدیر سرور در خط فرمان)، lint.sh (php -l)، botcheck.php
├── storage/               لاگ، بکاپ، به‌روزرسانی، رسیدها (خارج از گیت)
├── version.json           نسخه و بیلد (مقایسهٔ به‌روزرسانی روی build)
└── CHANGELOG.md           تاریخچهٔ تغییرات (ربات آخرین بلوک را اعلام می‌کند)
```

---

## 🔄 وظایف کران‌جاب

- همگام‌سازی مصرف سرویس‌ها با پنل‌ها و علامت‌گذاری کانفیگ‌های حذف‌شده از پنل
- هشدار انقضا و مصرف (روز/درصد قابل تنظیم)، غیرفعال/منقضی کردن و **حذف خودکار سرویس‌های منقضی پس از N روز**
- تمدید خودکار، پاک‌سازی سطل زباله، هشدار نودهای قطع
- بکاپ دوره‌ای + ارسال به تاپیک بکاپ، پاک‌سازی لاگ‌ها
- نرخ ارز خودکار، پیگیری فاکتورهای ارزی، بررسی رسیدهای معلق
- گزارش شبانه، **بررسی نسخهٔ جدید گیت‌هاب**، اعلان بیلد تازه، **اسکن خودکار دکمه‌های ربات**

---

## 🛡 نکات امنیتی

- پوشهٔ `install/` پس از نصب حذف شود (نصاب خودش تلاش می‌کند).
- ورود دومرحله‌ای پنل وب را روشن کنید و برای هر مدیر شناسهٔ تلگرام ثبت کنید.
- پوشه‌های `app`, `database`, `storage`, `cron` با `.htaccess` و قوانین `nginx.conf.sample` بسته شده‌اند.
- وب‌هوک با `secret_token` محافظت می‌شود؛ درخواست‌های جعلی رد می‌شوند.
- گزارش مشکلات امنیتی: [SECURITY.md](SECURITY.md)

---

## ❓ عیب‌یابی سریع

| مشکل | راه‌حل |
|---|---|
| ربات پاسخ نمی‌دهد | تنظیمات → **تنظیم وب‌هوک**؛ دامنه باید SSL معتبر داشته باشد |
| تست اتصال پنل ناموفق | پورت، پچ (web path)، یوزر/پسورد و فایروال پنل را بررسی کنید |
| PasarGuard: «ساخت کاربر بدون گروه ممکن نیست» | در پنل PasarGuard گروه بسازید یا شناسهٔ گروه را در فیلد «کد اینباندها» وارد کنید؛ برای کاربر غیر sudo دسترسی `groups.read` بدهید |
| مصرف کاربران صفر می‌ماند | کران‌جاب تنظیم نشده است |
| رسید پرداخت باز نمی‌شود | سرور به `api.telegram.org` دسترسی ندارد |
| به‌روزرسانی: «فایل version.json در مخزن نیست» | مخزن/شاخه/پوشهٔ داخلی را در تب «منبع» بررسی کنید؛ مخزن خصوصی توکن می‌خواهد؛ «جزئیات بررسی» کد HTTP را نشان می‌دهد |
| Cloudflare Error 1014 | رکورد CNAME به دامنهٔ دیگری در Cloudflare اشاره می‌کند؛ از رکورد A یا دامنهٔ خود استفاده کنید |
| `Something went wrong (invalid character 'C')` | پاسخ پنل HTML است نه JSON — آدرس/پچ پنل یا صفحهٔ ورود را بررسی کنید |

ابزار بررسی سینتکس: `bash tools/lint.sh`

---

## 💾 پشتیبان‌گیری

- پنل مدیریت → **بکاپ**: بکاپ دیتابیس (SQL) یا کامل (ZIP)، بکاپ خودکار هر N ساعت، نگه‌داری N فایل، ارسال به تاپیک بکاپ، بازگردانی از فهرست یا فایل آپلودی
- پیش از هر به‌روزرسانی یک بکاپ خودکار گرفته می‌شود

---

## 🤝 مشارکت و انتشار

- راهنمای مشارکت: [CONTRIBUTING.md](CONTRIBUTING.md)
- راهنمای نصب خودکار و دستور sr-ui: [docs/INSTALL-SR-UI.md](docs/INSTALL-SR-UI.md)
- راهنمای انتشار نسخه: [docs/RELEASE.md](docs/RELEASE.md)
- تاریخچهٔ تغییرات: [CHANGELOG.md](CHANGELOG.md)
- CI: بررسی سینتکس روی PHP 8.1 تا 8.3 و PHPStan؛ با زدن تگ `v*` زیپ ریلیز + `SHA256SUMS` خودکار منتشر می‌شود.

## 📄 مجوز

این سورس رایگان است؛ استفاده، تغییر و بازنشر آن آزاد و **فروش یا بازفروش خودِ سورس ممنوع** است. متن کامل: [LICENSE](LICENSE)

مجوز این پروژه هنوز نهایی نشده است؛ تا اعلام رسمی، همهٔ حقوق برای SRNetWork محفوظ است.
